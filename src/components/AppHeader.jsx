// SRP: Renders the app header — nothing else.

export default function AppHeader() {
  return (
    <header className="header">
      <h1>
        Spend<span>.</span>Track
      </h1>
      <p>Personal finance — clean &amp; simple</p>
    </header>
  );
}
